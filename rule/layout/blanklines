blank lines assert separation. their count signals the strength of the
break — match it to the structural level. in personal code, 1 line max;
shared code follows the language convention, `general/naming/audience`
(python: 2 between top-level definitions). in end-user content, 1 between
all blocks; hierarchy is carried by heading markers, not blank count.

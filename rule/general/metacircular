the tree answers to its own leaves. pass it through itself — a clean
reflection is the acceptance test. what fails indicts the rule or the
practice; either way the edit is known.

`general/reference:/checks this tree/` is the shape: the leaf ships the
check that checks it. a rule the tree cannot run on itself is opinion.

check: ~/src/sys/bin/check — every check the tree ships, one run.

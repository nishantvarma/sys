count the seconds from edit to seen. that number multiplies by every
change you will ever make — it outranks anything that runs once.

`general/measurefirst` is the program's speed. this is yours.

the biggest cut available to it is
`programming/standalone:/the host gone/` — the host's boot leaves the
loop entirely.

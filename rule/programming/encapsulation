abstraction is a barrier, not a layer. through a layer you still see the
representation; through a barrier you cannot.

the test: swap the representation. if nothing above notices, the barrier
is real.

put it where you expect change. generalise on the second instance — the
first is a case, not a pattern.

the barrier stops what is above from seeing down.
`programming/standalone:/the host gone/` is the same cut turned around —
nothing below reaching up. both, and the unit lifts out.

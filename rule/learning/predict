commit to the answer before you look. the gap between your guess and the
page is the only part that teaches; reading first closes it silently.

`learning/hindsight` is what you get when you skip this.
